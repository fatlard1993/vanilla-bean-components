build

# Prettier's markdown prose-wrap joins adjacent non-blank lines — it merges a `>` module
# pointer or `## Header` into a following `**method:**`/`**setup:**`/`**companion:**`
# annotation when there's no blank line between them, which breaks the LLD grammar's
# line-based parsing. Discovered when a `bun run format` pass silently merged
# `> styled/styled.js` and `**companion:** ...` onto one line.
**/*.lld.md
